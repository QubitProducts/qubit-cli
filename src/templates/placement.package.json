{
  "dependencies": {
    "@qubit/utils": "^1.6.0",
    "preact": "^10.5.14"
  },
  "devDependencies": {
    "@qubit/jest": "^1.4.0",
    "healthier": "^4.0.0",
    "jest": "^26.6.3",
    "prettier-standard": "^16.4.1"
  },
  "healthier": {
    "globals": [
      "jest",
      "expect",
      "test",
      "describe",
      "beforeEach",
      "beforeAll",
      "afterEach",
      "afterAll",
      "Element"
    ]
  },
  "jest": {
    "transform": {
      ".*(.js|.css|.less)$": "@qubit/jest"
    },
    "transformIgnorePatterns": []
  },
  "scripts": {
    "format": "prettier-standard --fix",
    "lint": "healthier",
    "test": "jest --coverage"
  }
}
